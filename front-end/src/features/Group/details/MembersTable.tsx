import React from 'react';
import { observer } from 'mobx-react-lite';

const MembersTable = () => {
    return (
        <div>
            <h1>Members Table</h1>
        </div>
    );
}

export default observer(MembersTable);