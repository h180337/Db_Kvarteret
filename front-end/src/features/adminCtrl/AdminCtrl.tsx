import React from 'react';
import { observer } from 'mobx-react-lite';

const AdminCtrl = () => {
    return (
        <div>
        Admin Ctrl page
        </div>
    );
}

export default observer(AdminCtrl);